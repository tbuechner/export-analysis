function examples_cycle(order) {
    const year = order.get('cf.cplace.solution.okr.year');
    const quarter = order.get('cf.cplace.solution.okr.quarter');
    const status = order.get('cf.cplace.solution.okr.status');
    const cyclesDashboard = order.get('cf.cplace.solution.okr.cyclesDashboard');
    const start = order.get('cf.cplace.solution.okr.start');
    const end = order.get('cf.cplace.solution.okr.end');
    const statusForNameGenerationPattern = order.get('cf.cplace.solution.okr.statusForNameGenerationPattern');
}


//------------------------------------------------------------------------------------------------------

function examples_objective(order) {
    const number = order.get('cf.cplace.solution.okr.number');
    const title = order.get('cf.cplace.solution.okr.title');
    const set = order.get('cf.cplace.solution.okr.set');
    const accomplished = order.get('cf.cplace.solution.okr.accomplished');
    const cycle = order.get('cf.cplace.solution.okr.cycle');
    const description = order.get('cf.cplace.solution.okr.description');
}


//------------------------------------------------------------------------------------------------------

function examples_keyResult(order) {
    const number = order.get('cf.cplace.solution.okr.number');
    const title = order.get('cf.cplace.solution.okr.title');
    const progressIndicator = order.get('cf.cplace.solution.okr.progressIndicator');
    const confidenceLevel = order.get('cf.cplace.solution.okr.confidenceLevel');
    const gradingForecast = order.get('cf.cplace.solution.okr.gradingForecast');
    const smallSupport = order.get('cf.cplace.solution.okr.smallSupport');
    const bigSupport = order.get('cf.cplace.solution.okr.bigSupport');
    const giveBigSupport = order.get('cf.cplace.solution.okr.giveBigSupport');
    const receiveBigSupport = order.get('cf.cplace.solution.okr.receiveBigSupport');
    const objective = order.get('cf.cplace.solution.okr.objective');
    const progress = order.get('cf.cplace.solution.okr.progress');
    const set = order.get('cf.cplace.solution.okr.set');
    const cycle = order.get('cf.cplace.solution.okr.cycle');
    const organizationalUnit = order.get('cf.cplace.solution.okr.organizationalUnit');
    const lastUpdate = order.get('cf.cplace.solution.okr.lastUpdate');
}


//------------------------------------------------------------------------------------------------------

function examples_progress(order) {
    const result = order.get('cf.cplace.solution.okr.result');
    const problem = order.get('cf.cplace.solution.okr.problem');
    const lessonsLearned = order.get('cf.cplace.solution.okr.lessonsLearned');
    const nextSteps = order.get('cf.cplace.solution.okr.nextSteps');
    const keyResult = order.get('cf.cplace.solution.okr.keyResult');
    const cycle = order.get('cf.cplace.solution.okr.cycle');
    const objective = order.get('cf.cplace.solution.okr.objective');
    const smallSupport = order.get('cf.cplace.solution.okr.smallSupport');
    const bigSupport = order.get('cf.cplace.solution.okr.bigSupport');
    const progressIndicator = order.get('cf.cplace.solution.okr.progressIndicator');
    const confidenceLevel = order.get('cf.cplace.solution.okr.confidenceLevel');
    const gradingForecast = order.get('cf.cplace.solution.okr.gradingForecast');
    const lastUpdate = order.get('cf.cplace.solution.okr.lastUpdate');
    const set = order.get('cf.cplace.solution.okr.set');
}


//------------------------------------------------------------------------------------------------------

function examples_task(order) {
    const title = order.get('cf.cplace.digitalBoard.title');
    const responsible = order.get('cf.cplace.digitalBoard.responsible');
    const description = order.get('cf.cplace.digitalBoard.description');
    const status = order.get('cf.cplace.digitalBoard.status');
    const dueDate = order.get('cf.cplace.digitalBoard.dueDate');
    const keyResult = order.get('cf.cplace.solution.okr.keyResult');
    const escalationLevel = order.get('cf.cplace.digitalBoard.escalationLevel');
}


//------------------------------------------------------------------------------------------------------

function examples_selectNextCycle(order) {
    const nextCycle = order.get('cf.cplace.solution.okr.nextCycle');
}


//------------------------------------------------------------------------------------------------------

