The following code contains a representation of a data model of an application:

```
{{ load_file('okr/generated/types-compressed.json') }}
```

The following code contains a representation of all layouts of this application, which represent the UI of the application:

```
{{ load_file('okr/generated/widgets-condensed-compressed.json') }}
```

Explain the UI of the application using the layouts and widgets.