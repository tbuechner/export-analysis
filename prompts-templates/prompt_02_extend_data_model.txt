The following code contains a representation of a data model of an application:

```
{{ load_file('ws_largeSolutionSAFe/generated/types-compressed.json') }}
```

Extend the data model with risk management capabilities. Generate the data model extension.